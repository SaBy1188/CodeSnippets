package de.basics;

public class SinnvollerCode {
    public static void main(String[] args) {
        int x = 1;
        int y = 2;
        String text = "Test";
        // eindeutige Variablennamen
        boolean untenRechts = (x == 3 || y == 2);
        boolean obenRechts = (x == 3 || y == 2);

        // statt Kommentare
        // Zeile unten rechts
        if (text != null) {
        }
        if (text != null) {
        }
    }
}